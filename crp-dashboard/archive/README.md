# Dashboard Archive

This directory stores dashboard artifacts that are not used in the current production build.

## Contents

- `README.create-next-app.md`
  - Original boilerplate README from `create-next-app`.
- `data_unused/cashflows/`
  - Unused scenario JSON files not referenced by current scenario set.

## Active source of truth

Use `crp-dashboard/README.md` and `crp-dashboard/src/data/` for active dashboard documentation and data.
