# Deprecated Docs (2026-03)

This folder stores documents that are no longer aligned with the current production pipeline.

## Archived files

- `MODEL_OVERVIEW.md`
  - Reason: described the old standalone `src.climada` physical module (flood/SLR-centric flow) and no longer matches the active PLANiT-integrated pipeline (`src/planit/*` + `src/pipeline/runner.py`).

## Policy

Archived documents are retained for historical traceability only and should not be used as the source of truth for current runs.
Current process documentation is in:

- `docs/MODEL_PROCESS_FULL.md`
