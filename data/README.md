# Data directory

- `raw/`: source inputs (plant parameters, policy assumptions, physical risk data).
- `external/`: third-party references (climate hazards, policy baselines).
- `processed/`: derived tables and model outputs. May be committed when used as
  reproducibility artifacts; not treated as automatically excluded.
